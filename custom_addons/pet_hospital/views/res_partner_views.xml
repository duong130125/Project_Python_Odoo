<odoo>
    <record id="view_partner_form_inherit_pet" model="ir.ui.view">
        <field name="name">res.partner.form.pet</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <div name="button_box" position="inside">
                <button name="action_view_pets" type="object" class="oe_stat_button" icon="fa-paw">
                    <field name="pet_count" widget="statinfo" string="Pets"/>
                </button>
            </div>
            <notebook position="inside">
                <page string="Thú cưng">
                    <field name="pet_ids">
                        <tree>
                            <field name="name"/>
                            <field name="type_id"/>
                            <field name="breed_id"/>
                            <field name="age"/>
                        </tree>
                    </field>
                </page>
            </notebook>
        </field>
    </record>
</odoo>
